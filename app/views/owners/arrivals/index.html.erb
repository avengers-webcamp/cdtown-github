
    <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>

<h2>入荷履歴</h2>

<div class="table">
	<thead>
		<tr>
			<th>入荷日時</th>
			<th>入荷数</th>
		</tr>
	</thead>
  <% @arrivals.each do |arr| %>
	<tbody>
		<tr>
			<p><th><%= arr.arrive_day %></th></p>
			<p><th><%= arr.arrive_count %></th></p>
		</tr>
	</tbody>
  <% end %>
</div>


<%= link_to '商品一覧',owners_cds_path %>

<%= link_to '商品登録',new_owners_cd_path %>

<%= link_to '入荷登録',new_owners_cd_arrival_path(@cd) %>