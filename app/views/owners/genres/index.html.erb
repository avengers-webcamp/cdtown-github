<div class="container">
 <div class="row">
  <div class="col-md-8">

  	<h3>ジャンル</h3>

  	<% @genres.each do |genre| %>
      <p><%= genre.name %></p>
    <%= link_to "削除", owners_genre_path(genre.id), method: :delete, "data-confirm" => "本当に削除しますか？", class:"btn-sm btn-danger destroy_book" %>
    <% end %>

  </div>
 </div>
</div>
